<resources>
    <style name="Theme.DroidTop" parent="Theme.AppCompat.Light.NoActionBar" />
    <!-- Screens the Gaming shell opens over itself: the shell is dark, so the
         window behind their content is too, instead of a white plate showing
         through before and around a dark Compose tree. -->
    <style name="Theme.DroidTop.Dark" parent="Theme.AppCompat.NoActionBar">
        <item name="android:windowBackground">@android:color/black</item>
    </style>
</resources>

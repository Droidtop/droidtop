<?xml version="1.0" encoding="utf-8"?>
<!-- Pre-31 fallback for res/drawable-v31/:
     @android:dimen/system_app_widget_background_radius arrived with the
     API-31 widget theming, and below that the reference does not resolve,
     so inflating the widget resize frame throws. The fork's own
     @dimen/enforced_rounded_corner_max_radius is the radius it clamps
     widget corners to on those platforms. -->
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">
    <solid android:color="@android:color/transparent" />
    <corners android:radius="@dimen/enforced_rounded_corner_max_radius" />
    <stroke android:width="2dp" android:color="?attr/workspaceAccentColor" />
</shape>
